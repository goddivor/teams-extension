{
  "name": "teams-extension",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && npm run copy-manifest",
    "copy-manifest": "copy public\\manifest.json dist\\",
    "preview": "vite preview",
    "watch": "vite build --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.1",
    "typescript": "~5.8.3",
    "vite": "^7.0.4",
    "vite-plugin-web-extension": "^4.4.5"
  }
}
